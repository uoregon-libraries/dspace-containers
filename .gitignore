# Ignore the source projects if present
/dspace-rest
/dspace-angular

# Ignore per-env compose config
/compose.override.yml
/.env

# Dev volume overrides (caddy conf.d for now)
/DEV-VOLS

/exports
!/export/db/.keep

# Built tools and assembled TPS templates (both fully derivable via make)
/bin
/tps-templates/challenge.go.html
/tps-templates/failed.go.html
